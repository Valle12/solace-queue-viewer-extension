{
  "name": "solace-queue-viewer-extension",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite",
    "build": "tsc && bce",
    "test": "vitest --coverage"
  },
  "devDependencies": {
    "bun-chrome-extension-dev": "0.1.8",
    "@types/bun": "latest"
  },
  "dependencies": {
    "material-design-lite": "^1.3.0",
    "solclientjs": "^10.17.0"
  }
}
