import { describe, expect, test } from "bun:test";
import { Solace } from "../src/solace";

describe("constructor", () => {
  // TODO use real tests
  test("test if solace exists", () => {
    expect(Solace).toBeDefined();
  });
});
